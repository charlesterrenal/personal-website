services:
  portfolio:
    image: ghcr.io/charlesterrenal/personal-website:latest
    container_name: portfolio
    ports:
      - "3001:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
